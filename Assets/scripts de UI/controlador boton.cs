using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class controladorboton : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

   public void boton() // no hay que colocar nada ya que se activa en un simple click
    {
        Debug.Log("he sido pulsado"); // dispara un mensaje en la consola para demostrar que a ocurrido un evento ej en este caso si a sido pulsado
    }
}
