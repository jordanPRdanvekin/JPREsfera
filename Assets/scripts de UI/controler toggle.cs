using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class controlertoggle : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void togle(bool pulsado) //cuando se activa el togle hay que usar una variable es decir el bool
    {
        Debug.Log("he sido pulsado"); // dispara un mensaje en la consola para demostrar que a ocurrido un evento ej en este caso si a sido pulsado
    }
}
